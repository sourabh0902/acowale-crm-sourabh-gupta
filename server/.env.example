PORT=
MONGODB_URI=use the standard-string instead of +srv
JWT_SECRET=your-jwt-secret-min-32-chars-use-crypto-randomBytes
JWT_EXPIRES_IN=
CORS_ORIGIN=
NODE_ENV=development

# --- Seed script (only used by scripts/seed.ts) ---
# The admin credentials the seed script will create.
# In production, override these to match what you'll share with reviewers.
SEED_ADMIN_EMAIL=admin@acowale.com
SEED_ADMIN_PASSWORD=Acowale@2026
